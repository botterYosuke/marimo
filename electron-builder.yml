appId: io.marimo.backcast
productName: Backcast
copyright: Copyright © 2026 Marimo

directories:
  buildResources: build
  output: release

files:
  - dist/**
  - electron/**
  - server/dist/**
  - server/backcast.py
  - server/server-manager.js
  - package.json
  - "!**/*.{iml,o,hprof,orig,pyc,pyo,rbc,swp,csproj,sln,xproj}"
  - "!.editorconfig"
  - "!**/._*"
  - "!**/{.DS_Store,.git,.hg,.svn,CVS,RCS,SCCS,.gitignore,.gitattributes}"
  - "!**/{__pycache__,thumbs.db,.flowconfig,.idea,.vs,.nyc_output}"
  - "!**/{appveyor.yml,.travis.yml,circle.yml}"
  - "!**/{npm-debug.log,yarn.lock,.yarn-integrity,.yarn-metadata.json}"
  - "!server/run.py"
  - "!server/backcast-server.spec"
  - "!server/build/**"
  - "!server/*.spec"

win:
  target:
    - target: nsis
      arch:
        - x64
  icon: frontend/public/logo.png
  artifactName: ${productName}-${version}-${arch}.${ext}

mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
  icon: frontend/public/android-chrome-512x512.png
  category: public.app-category.developer-tools
  artifactName: ${productName}-${version}-${arch}.${ext}

linux:
  target:
    - target: AppImage
      arch:
        - x64
  icon: frontend/public/logo.png
  category: Development
  artifactName: ${productName}-${version}-${arch}.${ext}

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true

publish: null

