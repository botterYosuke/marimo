# Python環境の管理

## パッケージマネージャー

このプロジェクトでは、Pythonパッケージの管理に **`uv`** を使用しています。

### 確認方法
- `uv.lock` ファイルが存在する
- `pyproject.toml` で `uv_build` がビルドバックエンドとして指定されている

### 使用時の注意事項
- 依存関係のインストールや管理には `uv` コマンドを使用すること
- `uv.lock` ファイルはコミット対象であり、手動で編集しないこと
- 新しい依存関係を追加する場合は `uv` を使用してプロジェクトを更新すること
